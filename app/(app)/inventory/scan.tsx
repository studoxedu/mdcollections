import React,{useState}from'react';import{Text,View}from'react-native';import{CameraView,useCameraPermissions}from'expo-camera';import{Screen,Button,Card}from'../../../src/components/ui';import{q}from'../../../src/database/client';export default()=>{const[perm,request]=useCameraPermissions(),[code,setCode]=useState('');if(!perm?.granted)return <Screen title="Barcode scanner"><Text>Camera permission is required to scan.</Text><Button title="Allow camera" onPress={()=>request()}/></Screen>;return <Screen title="Scan barcode" scroll={false}><View style={{height:500,margin:16,borderRadius:16,overflow:'hidden'}}><CameraView style={{flex:1}} facing="back" onBarcodeScanned={code?undefined:r=>setCode(r.data)}/></View>{code&&<Card><Text style={{fontWeight:'700'}}>Barcode found</Text><Text>{code}</Text><Button title="Find product" onPress={()=>{const p=q<any>('SELECT * FROM products WHERE barcode=?',[code])[0];setCode(p?code:'');}}/></Card>}</Screen>}

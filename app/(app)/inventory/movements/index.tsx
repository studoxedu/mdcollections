import React from'react';import{Text}from'react-native';import{useRouter}from'expo-router';import{Screen,Card,Button,Row}from'../../../../src/components/ui';import{q}from'../../../../src/database/client';import{dateTime}from'../../../../src/utils/format';export default()=>{const r=useRouter(),m=q<any>('SELECT m.*,p.name FROM stock_movements m LEFT JOIN products p ON p.id=m.product_id ORDER BY m.created_at DESC');return <Screen title="Stock movements"><Button title="+ Record movement" onPress={()=>r.push('/(app)/inventory/movements/new' as any)}/>{m.length?<>{m.map(x=><Row key={x.id} onPress={()=>r.push(`/(app)/inventory/movements/${x.id}` as any)}><Text style={{flex:1,fontWeight:'700'}}>{x.type} · {x.name}</Text><Text>{x.direction==='IN'?'+':'-'}{x.quantity} · {dateTime(x.created_at)}</Text></Row>)}</>:<Card><Text>No stock movements recorded.</Text></Card>}</Screen>}

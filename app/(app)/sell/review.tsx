import React,{useState}from'react';import{Text}from'react-native';import{useLocalSearchParams,useRouter}from'expo-router';import{Screen,Card,Button}from'../../../src/components/ui';import{money}from'../../../src/utils/format';export default()=>{const r=useRouter(),{cart}=useLocalSearchParams<{cart:string}>(),items:any[]=JSON.parse(cart||'[]'),[method,setMethod]=useState('Cash');const total=items.reduce((a,x)=>a+x.quantity*x.selling_price,0);return <Screen title="Review sale">{items.map(x=><Card key={x.id}><Text style={{fontWeight:'700'}}>{x.name}</Text><Text>{x.quantity} × {money(x.selling_price)}</Text></Card>)}<Card><Text style={{fontSize:24,fontWeight:'700'}}>{money(total)}</Text><Text>Payment method</Text>{['Cash','Transfer','POS','Other'].map(x=><Button key={x} title={x} secondary={method!==x} onPress={()=>setMethod(x)}/>)}</Card><Button title="Continue to payment" onPress={()=>r.push({pathname:'/(app)/sell/payment',params:{cart:cart||'[]',method}} as any)}/></Screen>}

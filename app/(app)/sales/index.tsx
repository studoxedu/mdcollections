import React from'react';import{Text}from'react-native';import{useRouter}from'expo-router';import{Screen,Card,Row}from'../../src/components/ui';import{q}from'../../src/database/client';import{money,dateTime}from'../../src/utils/format';export default()=>{const r=useRouter(),ss=q<any>('SELECT * FROM sales ORDER BY created_at DESC');return <Screen title="Sales history"><Card><Text style={{fontWeight:'700'}}>Today · Week · Month · Custom</Text><Text style={{fontSize:24,fontWeight:'700',marginTop:8}}>{money(ss.reduce((a,x)=>a+x.total,0))}</Text></Card>{ss.length?ss.map(s=><Row key={s.id} onPress={()=>r.push(`/(app)/sales/${s.id}` as any)}><Text style={{flex:1,fontWeight:'700'}}>{s.receipt_number}{'\n'}<Text style={{fontWeight:'400',color:'#5A6478'}}>{dateTime(s.created_at)} · {s.payment_method}</Text></Text><Text>{money(s.total)}</Text></Row>):<Card><Text>No sales yet.</Text></Card>}</Screen>}

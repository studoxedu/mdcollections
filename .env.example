# Google Sign-In "Web application" OAuth Client ID — used by
# @react-native-google-signin/google-signin for both authentication and
# Google Drive access (Drive is the sync backend; see README.md's
# "Google Drive sync setup" section for how to create this).
# Leave blank to run in offline demo mode with no sync.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

import {tx,run,one} from '../database/client'; import {IncomingEvent} from './types';
export const applyIncomingEvents=(events:IncomingEvent[])=>tx(()=>{for(const e of events){if(one('SELECT event_id FROM processed_events WHERE event_id=?',[e.event_id]))continue; const p=e.payload as any; switch(e.event_type){case 'PRODUCT_CREATED':case 'PRODUCT_UPDATED':run(`INSERT OR REPLACE INTO products(id,business_id,name,sku,barcode,category_id,supplier_id,cost_price,selling_price,current_stock,minimum_stock,unit,active,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`,[p.id,e.business_id,p.name,p.sku,p.barcode||null,p.category_id||null,p.supplier_id||null,p.cost_price,p.selling_price,p.current_stock,p.minimum_stock,p.unit||'pcs',p.active??1,p.created_at,p.updated_at]);break;case 'PRODUCT_DEACTIVATED':run('UPDATE products SET active=0,updated_at=? WHERE id=?',[new Date().toISOString(),e.entity_id]);break;case 'CATEGORY_CREATED':run('INSERT OR REPLACE INTO categories(id,business_id,name,created_at) VALUES(?,?,?,?)',[p.id,e.business_id,p.name,p.created_at]);break;case 'CUSTOMER_CREATED':case 'CUSTOMER_UPDATED':run('INSERT OR REPLACE INTO customers(id,business_id,name,phone,notes,created_at,updated_at) VALUES(?,?,?,?,?,?,?)',[p.id,e.business_id,p.name,p.phone||'',p.notes||'',p.created_at,p.updated_at]);break;case 'SUPPLIER_CREATED':case 'SUPPLIER_UPDATED':run('INSERT OR REPLACE INTO suppliers(id,business_id,name,phone,address,notes,created_at,updated_at) VALUES(?,?,?,?,?,?,?)',[p.id,e.business_id,p.name,p.phone||'',p.address||'',p.notes||'',p.created_at,p.updated_at]);break;case 'EXPENSE_CREATED':run('INSERT OR REPLACE INTO expenses VALUES(?,?,?,?,?,?,?,?,?,?,?,?)',[p.id,e.business_id,p.description,p.amount,p.category,p.date,p.note||'',p.user_id,p.device_id,p.event_id,e.client_created_at,0]);break;}run('INSERT INTO processed_events(event_id,server_sequence,processed_at) VALUES(?,?,?)',[e.event_id,e.server_sequence,new Date().toISOString()]);}});

export const schema = `PRAGMA journal_mode=WAL; PRAGMA foreign_keys=ON;
CREATE TABLE IF NOT EXISTS app_meta(key TEXT PRIMARY KEY,value TEXT); CREATE TABLE IF NOT EXISTS business(id TEXT PRIMARY KEY,name TEXT,currency TEXT,owner_user_id TEXT,created_at TEXT,updated_at TEXT);
CREATE TABLE IF NOT EXISTS members(id TEXT PRIMARY KEY,business_id TEXT,user_id TEXT,role TEXT CHECK(role IN ('OWNER','EMPLOYEE')),full_name TEXT,email TEXT,phone TEXT,active INTEGER,permissions TEXT,created_at TEXT,updated_at TEXT);
CREATE TABLE IF NOT EXISTS devices(id TEXT PRIMARY KEY,business_id TEXT,user_id TEXT,name TEXT,platform TEXT,revoked INTEGER,created_at TEXT,last_seen TEXT);
CREATE TABLE IF NOT EXISTS categories(id TEXT PRIMARY KEY,business_id TEXT,name TEXT,created_at TEXT); CREATE TABLE IF NOT EXISTS products(id TEXT PRIMARY KEY,business_id TEXT,name TEXT,sku TEXT,barcode TEXT,category_id TEXT,supplier_id TEXT,cost_price REAL,selling_price REAL,current_stock INTEGER,minimum_stock INTEGER,unit TEXT,active INTEGER,created_at TEXT,updated_at TEXT,image_local_uri TEXT,image_drive_id TEXT);
CREATE TABLE IF NOT EXISTS customers(id TEXT PRIMARY KEY,business_id TEXT,name TEXT,phone TEXT,notes TEXT,created_at TEXT,updated_at TEXT); CREATE TABLE IF NOT EXISTS suppliers(id TEXT PRIMARY KEY,business_id TEXT,name TEXT,phone TEXT,address TEXT,notes TEXT,created_at TEXT,updated_at TEXT);
CREATE TABLE IF NOT EXISTS sales(id TEXT PRIMARY KEY,business_id TEXT,receipt_number TEXT,customer_id TEXT,user_id TEXT,device_id TEXT,subtotal REAL,total REAL,payment_method TEXT,created_at TEXT,cancelled INTEGER); CREATE TABLE IF NOT EXISTS sale_items(id TEXT PRIMARY KEY,sale_id TEXT REFERENCES sales(id),product_id TEXT,product_name TEXT,quantity INTEGER,unit_price REAL,cost_price REAL,line_total REAL);
CREATE TABLE IF NOT EXISTS stock_movements(id TEXT PRIMARY KEY,business_id TEXT,product_id TEXT,type TEXT,direction TEXT,quantity INTEGER,previous_stock INTEGER,resulting_stock INTEGER,reason TEXT,note TEXT,user_id TEXT,device_id TEXT,event_id TEXT,created_at TEXT);
CREATE TABLE IF NOT EXISTS expenses(id TEXT PRIMARY KEY,business_id TEXT,description TEXT,amount REAL,category TEXT,date TEXT,note TEXT,user_id TEXT,device_id TEXT,event_id TEXT,created_at TEXT,deleted INTEGER);
CREATE TABLE IF NOT EXISTS audit_events(id TEXT PRIMARY KEY,business_id TEXT,user_id TEXT,device_id TEXT,action TEXT,entity_type TEXT,entity_id TEXT,payload TEXT,created_at TEXT); CREATE TABLE IF NOT EXISTS sync_events(event_id TEXT PRIMARY KEY,business_id TEXT,device_id TEXT,user_id TEXT,event_type TEXT,entity_id TEXT,payload TEXT,client_created_at TEXT,sync_status TEXT DEFAULT 'PENDING',retry_count INTEGER DEFAULT 0,last_error TEXT,server_sequence INTEGER);
CREATE TABLE IF NOT EXISTS processed_events(event_id TEXT PRIMARY KEY,server_sequence INTEGER,processed_at TEXT); CREATE TABLE IF NOT EXISTS sync_state(id INTEGER PRIMARY KEY CHECK(id=1),device_id TEXT,last_uploaded_sequence INTEGER DEFAULT 0,last_downloaded_sequence INTEGER DEFAULT 0,last_downloaded_cursor TEXT,last_successful_sync_at TEXT,pending_event_count INTEGER DEFAULT 0);
CREATE INDEX IF NOT EXISTS idx_products_b ON products(business_id); CREATE INDEX IF NOT EXISTS idx_products_n ON products(business_id,name); CREATE INDEX IF NOT EXISTS idx_products_sku ON products(business_id,sku); CREATE INDEX IF NOT EXISTS idx_products_bar ON products(business_id,barcode); CREATE INDEX IF NOT EXISTS idx_sales_b ON sales(business_id,created_at); CREATE INDEX IF NOT EXISTS idx_items_sale ON sale_items(sale_id); CREATE INDEX IF NOT EXISTS idx_mov_b ON stock_movements(business_id,created_at); CREATE INDEX IF NOT EXISTS idx_mov_p ON stock_movements(product_id); CREATE INDEX IF NOT EXISTS idx_exp_b ON expenses(business_id,date); CREATE INDEX IF NOT EXISTS idx_audit_b ON audit_events(business_id,created_at); CREATE INDEX IF NOT EXISTS idx_sync ON sync_events(sync_status,client_created_at);`;

import React from'react';import{ScrollView}from'react-native';export default({children}:{children:React.ReactNode})=><ScrollView horizontal showsHorizontalScrollIndicator={false}>{children}</ScrollView>;
